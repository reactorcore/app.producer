describe("Events service requests", function() {
  // testing GET request to /events
  // must return a JSON object
  // JSON object must contain a 'text' key
  it("responds with a JSON object upon GET request", function() {
    expect(true).toBe(true);
  });
});